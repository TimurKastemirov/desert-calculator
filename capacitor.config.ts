import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'dessert-calculator',
  webDir: 'dist/dessert-calculator',
  bundledWebRuntime: false
};

export default config;
